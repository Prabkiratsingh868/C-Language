#include <stdio.h>

int main(){
    int i = 0;
    while(i<10){
        printf("the value of i is %d\n" , i);
        i++;
    }
    return 0;
}

// If the condition never becomes false while loop executes the value 
// If the condition become zero the loop is terminates