#include <stdio.h>

int main(){
    int i = 1;
    do{
        printf("the value of i is %d\n" , i);
        i++;

    }while(2<=10); // The condition is 2<=10 and 2 is always less then 10 so it is infinite loop
}