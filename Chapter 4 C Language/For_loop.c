#include <stdio.h>

int main(){
    for(int i = 0; i<=10; i++){
        printf("The value of i is %d\n", i);
    }
    // i = 0 // intializing
    // i<=10 // condition test
    // i++ // increment it update the value of i 

    return 0;
}