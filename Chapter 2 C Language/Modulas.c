#include <stdio.h>

int main(){
    int a = -100;
    int b = 3;
    printf("The value when a is divided by b is %d" , a%b); // The sign of numerator is same as minus
    // It cannot applied on float
    // It returns the remainder
    // % is a module operator


    return 0;
}