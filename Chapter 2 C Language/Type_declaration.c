#include <stdio.h>

int main(){
    // int a;
    // a = 10;
    // int b = 2, c = 10 , d = 5; //This is also same and valid
    // printf("The value of a , b , c , d is %d, %d, %d, %d\n" , a ,b , c, d);

    //int e = a +2; //This is also an valid statement
    //printf("The value of e is %d\n" , e);

    // float i = 10;
    // float f = a + 2; // This is invalid statement

    int a , b , c , d;   // A valid statement
    a = b = c = d = 20;  // All value is equal to 20

    printf("The value of b is %d" , b);


    return 0;
}