// The airthmetic instructions are +,-,/,*,%

#include <stdio.h>

int main(){
    int a = 10;
    int b = 2;
    int c = a*b; //  * is airthmetic instruction
    printf("The value of c is %d" , c);

    // int d;
    // a*b = d;  This is invalid statement
    
    // We write d = a*b; This is valid
    
    // int d = a*b; This is valid
    // int d = ab; This is invalid we must use * operator


}