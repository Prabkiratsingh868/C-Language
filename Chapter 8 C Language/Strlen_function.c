// Strlen is the standard library function for strings

#include <stdio.h>
#include <string.h>

int main(){
    char st[] = "Prab";
    printf("%d" , strlen(st));

    // strlen function is used to get the length of the character string
    
    // This function is used to count the number of character excluding the null character (\0)

    return 0;
}