// Strcmp is the standard library function for strings

#include <stdio.h>
#include <string.h>

int main(){
    int a = strcmp("far" , "joke");
    // If the first word in dictionary is first so the value is negative
    // If the first word in dictionary is second so the value is positive

    printf("%d" , a);

    return 0;
}