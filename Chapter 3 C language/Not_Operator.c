#include <stdio.h>

int main(){
    int a = 0;
    int b = 1;
    printf("The value of not(a) is %d\n" , !a);
    printf("The value of not(b) is %d\n" , !b);

    // It changes the value into 0 to 1 and 1 to 0
    
    return 0;
}