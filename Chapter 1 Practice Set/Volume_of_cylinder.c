// Calculate the volume of cylinder given its radius and height

// the volume of cylinder is pie r square h
// the value of pie is 3.14

// r is radius and h is height

#include <stdio.h>

int main(){
    int r = 10;
    int h = 2;
    printf("The volume of cylinder with radius %d and height %d is %f" , r , h , 3.14*r*r*h);

    return 0;
}