// Array is the collection of similar elements

// Array allows the single variable to store multiple values

#include <stdio.h>

int main(){
    int marks[90]; // Reserve space to store 90 integers

    marks[0] = 85;
    marks[1] = 90;

    printf("The value of marks 0 and marks 1 is %d %d" , marks[0] , marks[1]);
    // We can go upto till the way marks[89]


    return 0;
}